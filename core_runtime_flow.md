User Query
 → Identify intent (Q&A / Quiz / Review)
 → Load user memory (past questions, weak topics)
 → PageIndex finds best chapter / section match
 → Extract structured text from section
 → Build grounded prompt with citations
 → LLM generates response
 → Store interaction in relational DB
 → Update memory & progress stats
 → Return answer